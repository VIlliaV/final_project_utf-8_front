import styled from 'styled-components';

export const CategoriesPageHead = styled.h2`
  color: rgba(0, 24, 51, 1);
  font-size: 28px;
  font-style: normal;
  font-weight: 600;
  line-height: 1;
  letter-spacing: -0.56px;

  margin: 50px 0;
`;
